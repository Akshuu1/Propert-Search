import AuthClient from "./AuthClient";

export const metadata = {
  title: "Domora - Auth",
  description: "Authentication page for Domora",
};

export default function AuthPage() {
  return <AuthClient />;
}
