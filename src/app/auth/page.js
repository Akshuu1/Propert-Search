import AuthClient from "./AuthClient";

export const metadata = {
  title: "Domora - Auth",
  description: "Authentication page for Espiro",
};

export default function AuthPage() {
  return <AuthClient />;
}
